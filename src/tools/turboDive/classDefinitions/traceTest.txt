[settings]
class = CTraceTest
fields = string enabled|\
    string number|\
    string method|\
    string name|\
    string params|\
    string fileName
includes = utillib.h
display_str = enabled, number, method, name, params
