---------------------------------------------------------------
blocks - retrieve one or more blocks from the chain or local cache
    handle_show
    handle_counts
        --count         display the number of the lists of appearances for --addrs or --uniq
    handle_uncles
        --uncles        display uncle blocks (if any) instead of the requested block
    handle_logs
        --logs          display only the logs found in the block(s)
        --emitter           for the --logs option only, filter logs to show only those logs emitted by the given address(es)
        --topic             for the --logs option only, filter logs to show only those with this topic(s)
        --articulate        for the --logs option only, articulate the retrieved data if ABIs can be found
        --big_range uint    for the --logs option only, allow for block ranges larger than 500 (default 500)
    handle_traces
        --traces        export the traces from the block as opposed to the block data
    handle_uniq
        --uniq          display a list of uniq address appearances per transaction
        --flow              for the --uniq option only, export only from or to (including trace from or to)
    handle_list
        --list          summary list of blocks running backwards from latest block minus num (hidden)
        --list_count        the number of blocks to report for --list option (hidden)
    handle_decache
        --decache       removes related items from the cache
    other_options:
        --hashes            display only transaction hashes, default is to display full transaction detail
        --cache_txs         force a write of the block's transactions to the cache (slow) (hidden)
        --cache_traces      force a write of the block's traces to the cache (slower) (hidden)
        --articulate        articulate the retrieved data if ABIs can be found
        --raw               report JSON data from the source with minimal processing

logs - retrieve logs for the given transaction(s)
    handle_show
    handle_decache
        --decache           removes related items from the cache
    other_options:
        --articulate        articulate the retrieved data if ABIs can be found
        --raw               report JSON data from the source with minimal processing

receipts - retrieve receipts for the given transaction(s)
    handle_show
    handle_decache
        --decache           removes related items from the cache
    other_options:
        --articulate        articulate the retrieved data if ABIs can be found
        --raw               report JSON data from the source with minimal processing

traces - retrieve traces for the given transaction(s)
    handle_show
    handle_filter
    handle_counts
    handle_decache
        --decache           removes related items from the cache
    other_options:
        --articulate        articulate the retrieved data if ABIs can be found
        --raw               report JSON data from the source with minimal processing

transactions - retrieve one or more transactions from the chain or local cache
    handle_show
    handle_logs
    handle_uniq
    handle_accountfor
    handle_source
    handle_decache
        --decache           removes related items from the cache
    other_options:
        --articulate        articulate the retrieved data if ABIs can be found
        --raw               report JSON data from the source with minimal processing

when - find block(s) based on date, blockNum, timestamp, or 'special'
    handle_show
    handle_list
    handle_ts_check     --timestamps --check    N/A
    handle_ts_count     --timestamps --count    N/A
    handle_ts_repair    --timestamps --repair   N/A
    handle_ts_show      --timestamps            N/A
    handle_ts_truncate  --timestamps --truncate N/A
    handle_ts_update    --timestamps --update   N/A
    handle_decache
        --decache           removes related items from the cache

---------------------------------------------------------------
export - export full detail of transactions for one or more addresses
    handle_show
    handle_appearance
    handle_logs
    handle_receipts
    handle_traces
    handle_statements
    handle_balances
    handle_neighbors
    handle_iterate
    handle_load
    handle_freshen
    handle_count
    handle_decache

---------------------------------------------------------------
slurp - fetch data from Etherscan for any address
    handle_show
    handle_appearances
    handle_decache

state - retrieve account balance(s) for one or more addresses at given block(s)
    handle_show
    handle_call
    handle_decache

tokens - retrieve token balance(s) for one or more addresses at given block(s)
    handle_show
    handle_parts
    handle_decache

---------------------------------------------------------------
status - report on the state of the internal binary caches
    handle_show
    handle_show_item
    handle_terse

---------------------------------------------------------------
COMMANDS THAT DO NOT HAVE A CACHE

list - list every appearance of an address anywhere on the chain
    handle_bounds
    handle_count
    handle_freshen
    handle_freshen_prod
    handle_list

abis - fetches the ABI for a smart contract
    handle_addresses
    handle_clean
    handle_encode
    handle_find

names - query addresses or names of well known accounts
    handle_autoname
    handle_clean
    handle_clean_test
    handle_crud
    handle_tags
    handle_terms
    handle_terms_test

chunks - manage, investigate, and display the Unchained Index
    handle_addresses
    handle_addresses_belongs
    handle_appearances
    handle_blooms
    handle_check
    handle_check_deep
    handle_check_hashes
    handle_check_internal
    handle_check_manifest
    handle_check_sequential
    handle_check_sizes
    handle_check_staging
    handle_dump
    handle_index
    handle_manifest
    handle_pin
    handle_pinorpublish
    handle_publish
    handle_stats
    handle_truncate

monitors - add, remove, clean, and list address monitors
    handle_clean
    handle_crud
    handle_freshen
    handle_list

daemon - initalize and control long-running processes such as the API and the scrapers
    handle_calls
    handle_grpc
    handle_monitor
    handle_scraper
    handle_sockets

init - initialize the TrueBlocks system by downloading the Unchained Index from IPFS
    handle_dryrun
    handle_init
    handle_init_prepare

scrape - scan the chain and update the TrueBlocks index of appearances
    handle_scrape
    handle_prepare
    handle_blaze_blaze
    handle_scrape_blaze
    handle_scrape_consolidate

explore - open a local or remote explorer for one or more addresses, blocks, or transactions

config - report on and edit the configuration of the TrueBlocks system
    handle_paths

---------------------------------------------------------------
