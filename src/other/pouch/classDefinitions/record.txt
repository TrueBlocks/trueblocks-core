[settings]
class = CRecord
fields = \
    uint32 key|\
    string date|\
    string type|\
    uint32 grant_id|\
    address address|\
    string name|\
    string slug|\
    uint32 cnt|\
    bool has_data
includes = etherlib.h|balance.h
display_str = key, date, type, address, name, cnt, csv, json
