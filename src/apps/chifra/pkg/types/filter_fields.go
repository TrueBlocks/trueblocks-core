package types

// Filter to specific fields if requested
// func filterFields(m map[string]any, extraOpts map[string]any) map[string]any {
// 	if fields, ok := extraOpts["fields"].([]string); ok {
// 		filtered := make(map[string]any)
// 		for _, field := range fields {
// 			if val, exists := m[field]; exists {
// 				filtered[field] = val
// 			}
// 		}
// 		return filtered
// 	}
// 	return m
// }
