[settings]
class    = CAccountName
fields   = string addr|string symbol|string name|string source|string description|string logo|bool isContract|string fn (nowrite)|double size (nowrite)|blknum lb (nowrite)|uint64 nrecs (nowrite)
includes = basetypes.h|basenode.h|sfarchive.h
