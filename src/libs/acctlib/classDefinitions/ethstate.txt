[settings]
class     = CEthState
fields    = blknum blockNumber|uint256 balance|uint64 nonce (nowrite)|string code (nowrite)|string storage (nowrite)|address address (nowrite)
includes  = etherlib.h
sort      = v1.blockNumber < v2.blockNumber
equals    = blockNumber == item.blockNumber
