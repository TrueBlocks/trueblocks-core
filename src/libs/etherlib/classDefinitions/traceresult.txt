[settings]
class = CTraceResult
fields = address newContract|\
        string code|\
        gas gasUsed|\
        string output
includes = abilib.h
cpp_includes = trace.h
serializable = true
equals = ((newContract == item.newContract) && (code == item.code) && (gasUsed == item.gasUsed) &&|(output == item.output))
