[settings]
class = CParameter
fields = \
    string type|\
    string name|\
    string str_default|\
    string value|\
    bool indexed (omitempty)|\
    string internalType|\
    CParameterArray components|\
    bool no_write (omitempty)|\
    bool omit_empty (nowrite,omitempty)|\
    uint64 is_flags=IS_ENABLED|\
    uint64 precision=5 (nowrite)
includes = utillib.h
display_str = type, name, str_default, value, internalType, indexed, is_pointer, is_array, is_object, is_builtin, no_write, omit_empty, is_minimal
tsx = true

#    parameter:
#      type: object
#      description: "a parameter to a function or event"
