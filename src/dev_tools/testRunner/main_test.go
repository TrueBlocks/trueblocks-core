package main

import (
	"testing"
)

func TestMainFunction(t *testing.T) {
	// os.Setenv("TEST _MODE", "true")
	// os.Setenv("TB_TEST_FILTER", "names")
	// err := os.Chdir("../../../build/")
	// if err != nil {
	// 	logger.Fatal(fmt.Sprintf("Failed to change directory: %v", err))
	// }
	// wd, _ := os.Getwd()
	// fmt.Println("Running from:", wd)
	// main()
}
