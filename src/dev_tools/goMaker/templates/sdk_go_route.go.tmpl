// Copyright 2016, 2024 The TrueBlocks Authors. All rights reserved.
// Use of this source code is governed by a license that can
// be found in the LICENSE file.
/*
 * Parts of this file were auto generated. Edit only those parts of
 * the code inside of 'EXISTING_CODE' tags.
 */

package sdk

import (
	// EXISTING_CODE
	// EXISTING_CODE
)

type {{toProper .Route}}Options struct {
{{range .Options}}	{{.GoSdkName}} {{.GoSdkType}} {{.JsonTag}}
{{end}}	Globals
}

// String implements the stringer interface
func (opts *{{toProper .Route}}Options) String() string {
	bytes, _ := json.Marshal(opts)
	return string(bytes)
}

// {{toProper .Route}} implements the chifra {{toLower .Route}} command for the SDK.
func (opts *{{toProper .Route}}Options) {{toProper .Route}}(w io.Writer) error {
	values, err := structToValues(*opts)
	if err != nil {
		log.Fatalf("Error converting {{toLower .Route}} struct to URL values: %v", err)
	}

	return {{.Pkg}}.{{toProper .Route}}(w, values)
}

// {{toLower .Route}}ParseFunc handles special cases such as structs and enums (if any).
func {{toLower .Route}}ParseFunc(target interface{}, key, value string) (bool, error) {
	var found bool
	{{.Enums3}}, ok := target.(*{{toProper .Route}}Options)
	if !ok {
		return false, fmt.Errorf("parseFunc({{toLower .Route}}): target is not of correct type")
	}

{{.Enums1}}
	// EXISTING_CODE
	// EXISTING_CODE

	return found, nil
}

// Get{{toProper .Route}}Options returns a filled-in options instance given a string array of arguments.
func Get{{toProper .Route}}Options(args []string) (*{{toProper .Route}}Options, error) {
	var opts {{toProper .Route}}Options
	if err := assignValuesFromArgs(args, {{toLower .Route}}ParseFunc, &opts, &opts.Globals); err != nil {
		return nil, err
	}

	return &opts, nil
}

func (opts *{{toProper .Route}}Options) Query() ([]{{.ReturnType}}, *rpc.MetaData, error) {
	buffer := bytes.Buffer{}
	if err := opts.{{toProper .Route}}(&buffer); err != nil {
		logger.Fatal(err)
	}

	var result Result[{{.ReturnType}}]
	if err := json.Unmarshal(buffer.Bytes(), &result); err != nil {
		return nil, nil, err
	} else {
		return result.Data, &result.Meta, nil
	}
}

{{if ne .Returns ""}}// {{.Returns}}{{end}}

{{.Enums2}}
// EXISTING_CODE
// EXISTING_CODE
