status?modes=transfers&maxRecords=100
{
  "errors": [
    "The mode option (transfers) must be one of [ index | blooms | blocks | transactions | traces | logs | statements | results | state | tokens | monitors | names | abis | slurps | staging | unripe | maps | some | all ]"
  ]
}
