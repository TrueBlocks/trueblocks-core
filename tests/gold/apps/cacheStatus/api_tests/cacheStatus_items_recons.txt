status?modes=recons&maxRecords=100
{
  "errors": [
    "The mode option (recons) must be one of [ index | blooms | blocks | transactions | traces | logs | statements | transfers | results | state | tokens | monitors | names | abis | slurps | staging | unripe | maps | some | all ]"
  ]
}
