chifra receipts  --file cmd_file.5 --no_header --fmt csv
TEST[DATE|TIME] NoHeader:  true
TEST[DATE|TIME] File:  cmd_file.5
TEST[DATE|TIME] Format:  csv
Cmd: 15644883 --no_header 
Cmd: 15644882 --no_header 
Cmd: 15644881 --no_header 

  15644883 does not appear to be a valid transaction index (one part, not first or latest). Quitting...

Purpose:
  Retrieve receipts for the given transaction(s).

Usage:
  getReceipts [flags] <tx_id> [tx_id...]

Arguments:
  transactions - a space-separated list of one or more transaction identifiers (required)

Flags:
  -a, --articulate   articulate the retrieved data if ABIs can be found

Global Flags:
  -x, --fmt string     export format, one of [none|json*|txt|csv|api]
  -v, --verbose uint   set verbose level (optional level defaults to 1)
  -h, --help           display this help screen

Notes:
  - The transactions list may be one or more transaction hashes, blockNumber.transactionID pairs, or a blockHash.transactionID pairs.
  - This tool checks for valid input syntax, but does not check that the transaction requested actually exists.
  - If the queried node does not store historical state, the results for most older transactions are undefined.

