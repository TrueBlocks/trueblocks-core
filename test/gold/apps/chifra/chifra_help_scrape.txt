chifra argc: 3 [1:scrape] [2:--help] 
chifra scrape --help 
chifra scrape argc: 2 [1:--help] 
chifra scrape --help 

  Usage:    chifra scrape [-n|-p|-v|-h]  
  Purpose:  Decentralized blockchain scraper and block cache.

  Where:  
	-n  (--n_blocks <num>)maximum number of blocks to process (defaults to 5000)
	-p  (--pin)           pin newly created chunks (and blooms) to Pinata (available only when scraping, requires API key)

#### Hidden options (shown during testing only)
	-b  (--n_block_procs <num>)number of block channels for blaze
	-a  (--n_addr_procs <num>)number of address channels for blaze
	-l  (--listpins)      show a list of all previously pinned chunks and blooms (precludes other options, requires API key)
#### Hidden options (shown during testing only)

	-v  (--verbose)       set verbose level. Either -v, --verbose or -v:n where 'n' is level
	-h  (--help)          display this help screen

  Powered by QBlocks
