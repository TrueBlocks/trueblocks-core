status?modes=ripe&maxRecords=100
{
  "errors": [
    "The mode option (ripe) must be one of [ index | blooms | blocks | transactions | traces | logs | statements | state | tokens | monitors | names | abis | slurps | staging | unripe | maps | some | all ]"
  ]
}
