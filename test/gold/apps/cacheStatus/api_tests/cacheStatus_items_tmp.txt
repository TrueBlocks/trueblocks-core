status?modes=tmp&maxRecords=100
{
  "errors": [
    "The mode option (tmp) must be one of [ index | blooms | blocks | txs | traces | monitors | names | abis | recons | slurps | staging | unripe | maps | some | all ]"
  ]
}
