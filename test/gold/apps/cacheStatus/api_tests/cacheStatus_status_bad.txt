status?modes=config&verbose
{
  "errors": [
    "The mode option (config) must be one of [ index | blooms | blocks | txs | traces | monitors | names | abis | recons | slurps | staging | unripe | maps | some | all ]"
  ]
}
