status?modes=config&verbose
{
  "errors": [
    "The mode option (config) must be one of [ abis | blocks | monitors | names | recons | slurps | tmp | traces | txs | blooms | index | ripe | staging | unripe | maps | some | all ]"
  ]
}
