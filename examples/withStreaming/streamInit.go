package main

func TestStreamInit() {
	// TODO: Add streaming examples
	// InitAll ([]types.Message, *types.MetaData, error) {
	// InitDryRun ([]types.Message, *types.MetaData, error) {
	// InitExample(val string) ([]types.Message, *types.MetaData, error) {
}
